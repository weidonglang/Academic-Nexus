# v1.4.1 Final Closure Report

Updated: 2026-06-28 09:04 +08:00

Scope: continue from PR #102 and close the remaining verification gaps for #76-#104. This pass does not publish a GitHub Release and does not close GitHub issues.

## PR #102 Recheck

PR #102 already stabilized AI model selection, sensitive words, load-test offering parsing, auth sessions, authenticated downloads, menu routing, database masking, system health, database browser partial failures, post-review attachment immutability, dynamic current term, schedule parsing, and grab validation. This pass kept those paths intact and reran the full backend test suite.

## New Fixes In This Pass

| Issue | Result | Evidence |
| --- | --- | --- |
| #79 | Added real CSV preview/commit endpoints and UI for batch user import. Student rows create `sys_user`, `sys_user_role`, `student`, batch task, audit logs, and login-capable accounts. | `AdminBulkClosureController`, `UserAdminView.vue`, `BatchUserImportClosureTests` |
| #80 | Added real CSV preview/commit endpoints and UI for course/offering import. Offering import validates teacher, term, capacity, schedule, selection window, writes batch task/audit, and prewarms Redis stock when Redis is available. | `AdminBulkClosureController`, `CourseOfferingAdminView.vue`, `BatchCourseOfferingImportClosureTests` |
| #81 | Added batch review APIs and UI for status-change and registration applications with skip/partial-success details, batch task, notifications, cache eviction, and audit. | `AdminBulkClosureController`, status/registration admin views, `BatchReviewClosureTests` |
| #82 | Added target preview and publish support for all/role/student/teacher/admin/grade/major/class/offering notices, with zero-recipient guard and targeted audit. | `AdminBulkClosureController`, `AdminNoticeController`, `NoticeAdminView.vue`, `NotificationTargetingClosureTests` |
| #84 | Added teacher read-only awareness APIs for homeroom class applications and course-related registration summaries. | `TeacherApplicationAwarenessController`, `TeacherApplicationAwarenessClosureTests` |
| #83 | Admin locked-grade modification now records old/new values, reason, trace/audit, high-risk action, and student notification. Existing teacher locked-grade guard remains. | `AcademicAdminController`, `AcademicAdminMapper` |
| #86 | Added batch task detail API and non-server-path report URL. | `BatchAndArchiveController`, `batchOps.ts` |
| #89 | Data archive wording and API now consistently state demo-safe mode. | `BatchAndArchiveController`, `DataArchiveView.vue` |
| #94 | File delete failures write failure audit before surfacing the error. | `AdminFileController` |
| #95 | Redis repair requires confirmation, records before/after report details, and returns degraded conflict if Redis is unavailable. | `CourseSelectionConsistencyController` |
| #101 | Grab request validates required `offeringId`, requestId length, and requestId characters. | `CourseSelectionController`, `CourseGrabValidationRegressionTests` |
| #103 | Dockerfiles now use BuildKit syntax, Maven cache mounts, `dependency:go-offline`, mirror configuration, and build scripts. Docker build passed. | `Dockerfile`, `ai-service/Dockerfile`, `docker/maven/settings.xml`, `scripts/docker-build.*` |
| #104 | Compose host ports are configurable and default away from common conflicts. Nacos startup ordering was hardened after smoke testing exposed an AI service startup race. | `docker-compose.yml`, `.env.example`, `scripts/check-ports.*` |

## Three-Role Data Flow

| Flow | Student | Teacher | Admin | Result |
| --- | --- | --- | --- | --- |
| Batch user import | Imported student can log in and has class profile | Imported teacher receives TEACHER role when provided | Admin preview/commit writes user/profile/role | PASS |
| Batch offering import | Student selection page can see imported offering by term | Teacher is required to be an enabled TEACHER account | Admin course/offering list is DB-backed | PASS |
| Batch review | Student application status and notification update | Teacher read-only cache is evicted | Admin receives partial success and task ID | PASS |
| Targeted notice | Target users receive notification only | Role/teacher targets supported | Admin sees preview count and targeted audit | PASS |
| Teacher awareness | Students cannot access teacher endpoints | Homeroom/course teachers see summaries only | Admin review flow unchanged | PASS |

## Batch Task Results

| Task Type | Producer | Detail/Report |
| --- | --- | --- |
| `USER_IMPORT` | `/api/admin/users/import-commit` | Batch task row plus report endpoint |
| `COURSE_IMPORT` | `/api/admin/courses/import-commit` | Batch task row plus report endpoint |
| `COURSE_OFFERING_IMPORT` | `/api/admin/course-offerings/import-commit` | Batch task row plus report endpoint |
| `STATUS_CHANGE_BATCH_REVIEW` | `/api/admin/status-changes/batch-review` | Partial-success items and task detail |
| `REGISTRATION_APPLICATION_BATCH_REVIEW` | `/api/admin/registration-applications/batch-review` | Partial-success items and task detail |
| `ARCHIVE_*` / `CLEANUP_*` | data archive APIs | Demo-safe record plus batch task |

## Notification Events

| Event | Notification |
| --- | --- |
| Status-change batch approval/rejection | Student receives application result |
| Registration batch approval/rejection | Student receives application result |
| Admin targeted notice | Only resolved target users receive notice |
| Grade update | Student receives grade-change notice |
| Exam create/update/delete | Existing admin/teacher paths notify selected students |

## Grade Audit

Admin grade updates now load the previous grade row before modification, update score/status/grade point, insert `grade_change_log`, write `HIGH_RISK_GRADE_UPDATE` for locked rows, and notify the affected student. Teacher locked-grade restrictions remain covered by existing flow tests.

## Docker Verification

| Command | Result |
| --- | --- |
| `docker compose config` | PASS |
| `powershell -ExecutionPolicy Bypass -File .\scripts\check-ports.ps1` | PASS, all default ports free |
| `powershell -ExecutionPolicy Bypass -File .\scripts\docker-build.ps1` | PASS, main/AI/frontend images built |
| `docker compose up -d` | PASS after Nacos startup-order hardening |
| `Invoke-WebRequest http://localhost:5174` | PASS, 200 |
| `Invoke-WebRequest http://localhost:8088` | PASS, 200 |
| `Invoke-WebRequest http://localhost:18090/internal/ai/status` | PASS |

The first compose smoke test exposed `academic-ai-service` exiting with Nacos client status `STARTING`. The fix changed Nacos dependencies to `service_healthy` and added `SPRING_CLOUD_NACOS_DISCOVERY_FAIL_FAST=false` for the Java services.

## Test Commands

| Command | Result |
| --- | --- |
| `.\mvnw.cmd test` | PASS, 49 tests |
| `.\mvnw.cmd -f ai-service/pom.xml test` | PASS, no test sources |
| `cd frontend; npm run build` | PASS |
| `py -m pytest scripts/tests -q` | PASS, 5 tests |
| `docker compose config` | PASS |
| `.\scripts\docker-build.ps1` | PASS |
| `docker compose up -d` | PASS |

Note: `python -m pytest` initially failed because the WindowsApps Python launcher was inaccessible and the installed Python lacked pytest. `pytest` was installed for the local Python launcher and the required script tests then passed.

## Regression Confirmation

| Issues | Status |
| --- | --- |
| #76 #77 #78 #90 #91 #92 #93 #96 #97 #98 #99 #100 | Rechecked through full backend suite, frontend build, script tests, and Docker smoke |
| #87 #88 | Existing three-role exam/evaluation paths remain covered by `SystemInteroperabilityIntegrationTests` and `CourseFlowIntegrationTests` |

## Not In Scope

The following remain intentionally excluded from v1.4.1 final closure: smart lesson/resource library, database backup/restore, new large modules, and GitHub Release/issue closing before PR merge.

## Closure Conclusion

#76-#104 are now ready for maintainer review in a PR. Do not close the issues or publish `v1.4.1-final-closure` until the PR is merged and the release checklist is repeated on `main`.
