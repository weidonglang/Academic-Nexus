# EduNexus AI v2.0.1 QA Report

Updated: 2026-06-29

## 1. Scope

This report tracks the v2.0.1 stabilization scope for EduNexus AI.

Covered items:

- #109 SearXNG Docker network configuration documentation
- #110 Ollama enablement for ai-service Docker deployment documentation
- #111 Web-search grounding before model generation
- #112 AI model registry default/deleted/enabled state constraints
- #114 Top global search and quick-app settings interactions
- #115 AI Chat frontend experience improvements
- #113 duplicate of #114
- Project display name update from Academic-Nexus to EduNexus AI
- AI thinking mode switch: Auto / On / Off
- Local and Docker run verification

## 2. Duplicate Issue Handling

#113 duplicates #114. The implementation should be tracked under #114 only.

## 3. Branding Verification

- [x] README title uses **EduNexus AI**.
- [x] README description contains **AI-powered Academic Management Platform**.
- [x] Frontend browser title uses **EduNexus AI**.
- [x] Login page title uses **EduNexus AI**.
- [x] Main shell brand uses **EduNexus AI**.
- [x] Footer uses **EduNexus AI · AI-powered Academic Management Platform · v2.0.1**.
- [x] AI service prompts refer to **EduNexus AI** and Chinese name **智教中枢**.
- [x] Docker deployment guide title uses **EduNexus AI Docker Deployment Guide**.
- [x] AI search and safety guide title uses **EduNexus AI Search And Safety Configuration**.
- [x] CHANGELOG records the branding update.
- [x] Release notes draft contains a Branding section.

Compatibility notes:

- Java package names are unchanged.
- API paths are unchanged.
- Flyway historical migrations are unchanged.
- Docker service names such as `academic-main` and `academic-ai-service` are unchanged.
- Repository path remains `weidonglang/Academic-Nexus` unless the maintainer renames it manually.

## 4. Verification To Complete Before Release

Commands to run locally:

```powershell
.\mvnw.cmd clean test
.\mvnw.cmd -f ai-service/pom.xml clean test

cd frontend
npm ci
npm run build
cd ..

py -m pytest scripts/tests -q

docker compose config
.\scripts\check-ports.ps1
.\scripts\docker-build.ps1
docker compose up -d --build
docker compose ps
```

Manual smoke tests:

- [ ] Frontend opens at `http://localhost:5174`.
- [ ] Main service opens at `http://localhost:8088`.
- [ ] AI service status opens at `http://localhost:18090/internal/ai/status`.
- [ ] Login page displays EduNexus AI.
- [ ] AI Chat introduces the system as EduNexus AI.
- [ ] Docker docs are readable and still use historical service names where required.
- [ ] Top global search works after #114 implementation.
- [ ] Quick-app settings work after #114 implementation.
- [ ] Thinking mode works after the v2.0.1 AI Chat implementation.

## 5. Release Readiness

This draft report documents the branding update and v2.0.1 verification checklist. The maintainer should only publish v2.0.1 after all automated tests and smoke tests pass.
